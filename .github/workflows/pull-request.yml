name: pull request test pipeline

run-name: ${{github.actor}} is using a ${{github.workflow}} on branch ${{github.ref}}

on:
  push:
    branches: [develop]
  pull_request:
    branches: [develop]

jobs:
  tests:
    uses: ./.github/workflows/tests.yml
